<section class="cart">
    <h2 class="cart__title">{{ section.settings.cart_title }}</h2>
    <form action="/cart" method="post" class="cart__wrapper">
        <div class="cart__items">
            {% for line_item in cart.items %}
                {% render 'line-item', line_item: line_item, index: forloop.index %}
            {% endfor %}
        </div>

        <div class="cart__details">
            <div class="cart__total">
                <strong class="cart__total-text">Total:</strong>
                <span class="cart__total-price">{{ cart.total_price | money }}</span>
            </div>
            <div class="cart__btns">
                <button class="button" type="submit">Update Quantities</button>
                <button class="button" type="submit" name="checkout">Checkout</button>
            </div>
        </div>
    </form>
</section>

{% schema %}
{
    "name": "cart",
    "settings": [
        {
            "type": "text",
            "id": "cart_title",
            "label": "Cart title",
            "default": "Your Cart"
        }
    ]
}
{% endschema %}